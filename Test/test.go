package Test

/*import (
	"github.com/stretchr/testify/require"
	"log"
	"testing"
)

var flagTests []struct {
	discritption string
	out          []string
}

func TestRestaurant(t *testing.T) {
	for _, tt := range flagTests {
		t.Run(tt.discritption, func(t *testing.T) {
			s, err := generalUniq(tt.in, tt.opt)
			if err != nil {
				log.Fatal(err)
			}
			require.Equal(t, tt.out, s, "The two result should be the same.")
		})
	}
}
*/